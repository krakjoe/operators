// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("operators", "for operators support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("operators", "enable operators support", "no");

if (PHP_OPERATORS != "no") {
	EXTENSION("operators", "operators.c");
}

